<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head') %> <%# incluimos el head comun %>
  
  <head>
    <title>My Profile | Sweet Avenue</title>
    <link rel="stylesheet" href="/css/myProfile.css"> <%# incluimos el css solo para mi perfil%>
  </head> 

  <body>
    <%- include('partials/headerLoggedOn') %>  <%# incluimos el header de cuando estamos logeados %>
    <div class="card user-card" style="width: 100;">
        <img src="/<%= user.image %>" alt=""> <%# incluimos la foto de perfil %>

        <div class="card-body">
          <h5 class="card-title"><%= user.name %> <%= user.surname %></h5> <%# incluimos nombre y apellido %>
          <h6 class="card-subtitle mb-2 text-muted">@<%= user.username %></h6> <%# incluimos el nombre de usuario  %>
          <p class="card-text"><%= user.biography %></p> <%# incluimos la bibliografia %>
          <p class="card-text follows">Followers: 100</p>
          <p class="card-text follows">Following: 100</p>
          <form action="/users/editprofile">
          <button type="submit" class="btn btn-secondary btn-sm">Edit Profile</button>
          </form>
        </div>
    </div>
    <main>
      <% for(post of userPosts) { %>  <%# for que recorre los posts del usuario %>
        <div class="card" style="width: 18rem;">
          <a class="streched-link" href="/posts/detail/<%= post.postId %>"> <%# incluimos el link para el detalle %>
          <img src="/<%= post.image %>" class="card-img-top" alt="...">
          </a> <%# incluimos la imagen de la torta %>
          <div class="card-body">
            <a class="streched-link" href="/posts/detail/<%= post.postId %>"> <%# incluimos link para ir al detalle %>
            <h4 class="post-title"><%= post.name %></h4> <%# incluimos el nombre de la torta %>
            </a>
            <a class="streched-link" href="/posts/detail/<%= post.postId %>"> <%# incluimos link para ir al detalle %>
            <p class="post-description card-text"><%= post.description %></p>
            </a> <%# incluimos la descripcion del posteo %>
            <p class="card-text post-date">Date: <%= post.date %></p> <%# incluimos el dia del posteo %>
            <hr>
            <%# los comentarios %>
            <% for (let comment of post.comments) { %>
              <a class="streched-link" href="/users/<%=comment.user.username%>"><p class="post-username-comment">@<%=comment.user.username%></p></a>
              <p class="post-comment-content"><%=comment.text%></p>
              <br>
            <% } %> 
          </div>
        </div>
      <% } %> 
    </main>

    <%- include('partials/footer') %> <%# incluimos el footer comun %>
  </body>
</html>