<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head') %> 

  <head>
    <title><%=post.name%> | Sweet Avenue</title>
    <link rel="stylesheet" href="/css/postDetail.css"> 
  </head> 

  <body>
    <%- include('partials/headerLoggedOn') %> 

    <div class="card post-detail">
      <div class="card-header title">
        <%= post.name %> 
      </div>
      <div class="card-header username">
        <a class="streched-link" href="/users/<%= post.username %>"> <%= post.username %> </a> 
      </div> 
      <div class="card-body">
        <section>
          <img src="/images/<%= post.picture %> " class="card-img-top" alt="..."> 
          <p class="card-text description"><%= post.description %></p> 
          <p class="card-text post-date">Date: <%= post.date_creation %></p> 
        </section>
        <section>
          <h5 class="card-title">Ingredientes</h5>
          <p class="card-text"> <%= post.ingredients%> </p> 
          <h5 class="card-title instructions-title">Instrucciones</h5>
          <p class="card-text"><%= post.instructions %> </p> 
        </section>
        <section> 
          <h5 class="card-title">Comments</h5>
          <% for(let comment of comments) { %>
            <a class="streched-link" href="/users/<%= comment.id_user %>"><p class="post-username-comment">@<%= comment.id_user %></p></a>
            <p class="post-comment-content"><%= comment.text%></p>
            <br>
          <% } %> 
          <h5 class="card-title leave-comment-title">Leave a comment</h5>
          <form class="form-comment">
              <div class="input-group">
                <textarea class="form-control" aria-label="With textarea"></textarea>
              </div>
              <button type="submit" class="btn btn-primary">Comment</button>
          </form>
        </section>
      </div>
    </div>
    <%- include('partials/footer') %> 
  </body>
</html>